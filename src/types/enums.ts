export enum ResponseType {
	SHORT_TEXT = "text",
	LONG_TEXT = "textarea",
	RADIO = "radio",
	CHECK_BOX = "checkbox",
	ROUND_BOX = "roundbox",
	BUBBLES = "bubbles",
	DATE = "date",
	TIME = "time",
}

export enum AdminRole {
	SUPER_ADMIN = "SUPER_ADMIN",
	ADMIN = "ADMIN",
}

export enum AdminStatus {
	ACTIVE = "ACTIVE",
	INACTIVE = "INACTIVE",
}
